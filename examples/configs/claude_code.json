{
  "$comment": "Add to .claude/settings.json (project) or ~/.claude/settings.json (global). Replace the command path with your actual binary location.",
  "mcpServers": {
    "serial": {
      "command": "/usr/local/bin/serial-mcp-server",
      "args": [
        "--allowlist=/dev/ttyACM*,/dev/ttyUSB*"
      ]
    }
  }
}
